# GitHub Actions — daily digest email (repository secrets)

# Set these in: Settings → Secrets and variables → Actions

DIGEST_EMAIL_TO=rahul.jha@itu.int
DIGEST_EMAIL_FROM=
SMTP_SERVER=smtp.office365.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

# See docs/DAILY-DIGEST.md for setup and ITU Microsoft 365 notes.
